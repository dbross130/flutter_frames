import 'package:flutter/material.dart';

class Dipensaryunit extends StatelessWidget {
  const Dipensaryunit({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      
    );
  }
}